BEGIN
FOR hdr IN(
SELECT MEMBER_CODE,STAFF_ID||'_'||FIRST_NAME ASTAFF FROM IM_MEMBERS WHERE STAFF_ID||'_'||FIRST_NAME IN(SELECT * FROM UNECP WHERE Length(PICNAME) <> '6'))LOOP
UPDATE IM_MEMBERS       SET MEMBERPHOTO = BFILENAME('MEMBER_PHOTO', '\0252-04\'||HDR.ASTAFF||'.jpg') WHERE MEMBER_CODE=hdr.MEMBER_CODE;
END LOOP;
END;
