SELECT DISTINCT DTL.CLAIM_CODE FROM IM_CLAIM_PROCESS_HEADER HDR,IM_CLAIM_PROCESS_DETAIL DTL
WHERE CLAIM_STATUS IN (1,2) AND NVL(HDR.AUTHORIZEDSTATUS,0) = 0
AND HDR.CLAIM_CODE = DTL.CLAIM_CODE
AND HDR.MAP_VALUE IS NOT NULL AND DTL.PRICE = -1