UPDATE ECLAIMS SET SYS_CLAIM_STATUS = 277 WHERE SENDER_ID IN
(SELECT OTHERLICNECEID FROM IM_PROVIDER_LICENCES 
WHERE PROVIDERCODE IN(SELECT PROVIDERCODE FROM 
IM_PROVIDERS WHERE PROVIDERID IN ('B105','KA02','FN02')))
AND RESUBMISSION_COMMENT IS NULL AND SYS_CLAIM_ID NOT IN
(SELECT MAP_VALUE FROM IM_CLAIM_PROCESS_HEADER WHERE MAP_VALUE IS NOT NULL)