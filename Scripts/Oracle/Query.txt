SELECT POLICYID,POLICYNAME,SUM(TOTAL) FROM IM_POLICYFINANCEPOSTING POS
JOIN IM_POLICY POL ON POL.POLICYCODE = POS.POLICYCODE
WHERE POS.AUTHORIZEDSTATUS = 0 AND NVL(PRODUCTIONSOURCE,0) <> 0
GROUP BY  POLICYID,POLICYNAME,POS.POLICYCODE