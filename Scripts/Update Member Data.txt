BEGIN
FOR HDR IN(SELECT * FROM IM_MEM_UPDATE) LOOP

UPDATE IM_MEMBERS SET EMIRATES_ID = HDR.EMIRATESID,UIDNUMBER = HDR.UIDNO,
PASSPORT_NO = HDR.PASSPORTNO WHERE CARDNO = HDR.CARDNO;
COMMIT;
UPDATE IM_MEMBERPOLICY SET A_EMIRATES_ID = HDR.EMIRATESID,UIDNUMBER = HDR.UIDNO,
A_PASSPORT_NO = HDR.PASSPORTNO WHERE ACARDID = HDR.CARDNO;
COMMIT;
END LOOP;
END;